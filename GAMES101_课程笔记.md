# GAMES101 计算机图形学课程笔记

## 学习路线

1. **表示与变换（01–04）**：向量/矩阵 → 模型、相机、投影。
2. **实时渲染（05–09）**：三角形光栅化 → 深度测试 → 局部光照 → 纹理与阴影。
3. **几何处理（10–12）**：网格、曲线曲面、细分和简化。
4. **离线渲染（13–18）**：光线求交 → 加速 → 辐射度量 → 渲染方程 → Monte Carlo → 材质。
5. **成像与颜色（19–20）**：相机成像、光场、颜色感知与颜色空间。
6. **运动与模拟（21–22）**：关键帧、弹簧系统、数值积分与物理模拟。

## 逐讲要点

### 01 计算机图形学概览

- 图形学应用：游戏、电影、动画、设计、可视化、VR/AR、仿真、医学和界面。
- 核心困难：用数学表示三维形状，用物理解释光照，用数值方法处理动画/模拟，并在硬件约束下高效生成图像。
- 四条主线：**Rasterization（光栅化）**、**Curves & Meshes（曲线和网格）**、**Ray Tracing（光线追踪）**、**Animation/Simulation（动画与模拟）**。
- 实时光栅化强调速度；电影级光线追踪强调准确的全局光照。

### 02 线性代数复习

- 向量：长度 `||v||`、单位化 `v/||v||`、加法和数乘。
- 点积：`a·b = ||a||||b||cosθ`；判断夹角方向、正交性和投影。投影 `proj_b(a) = (a·b)/(b·b) b`。
- 叉积：`a×b` 垂直于两向量，方向遵循右手定则；`||a×b||` 是平行四边形面积。
- 正交归一基、坐标系变换、矩阵乘法和行列式是后续变换与法线计算的基础。

### 03 二维变换与齐次坐标

- 缩放、旋转、切变可用 2×2 矩阵；平移不能由 2×2 矩阵统一表示。
- 齐次坐标：点写作 `(x,y,1)`，向量写作 `(x,y,0)`；用 3×3 矩阵统一平移、旋转、缩放和切变。
- 复合变换通过矩阵相乘实现，**右侧先执行**；顺序改变通常会改变结果。
- 仿射变换保持直线和平行性；欧氏变换额外保持距离和角度。

### 04 三维变换、观察与投影

- 三维齐次坐标使用 4×4 矩阵；绕 x/y/z 轴旋转可组合为 Euler 角（roll/pitch/yaw）。
- 相机参数：位置 `e`、观察方向 `g`、上方向 `t`。View 矩阵把相机移到原点、朝向 `-Z`、上方对齐 `+Y`。
- 正交投影：视锥体映射为规范立方体 `[-1,1]^3`，平行线保持平行。
- 透视投影：视锥体映射为规范立方体；深度非线性，近处物体更大。典型链路：`MVP = M_projection M_view M_model`，再做透视除法 `x,y,z /= w`。
- Viewport 将规范立方体的 xy 映射到屏幕像素范围 `[0,width]×[0,height]`。

### 05 光栅化：三角形

- 三角形是最基本且稳定的图元：一定共面、内部定义明确、顶点属性可插值。
- 光栅化流程：顶点变换 → 三角形装配 → 覆盖测试（point-in-triangle）→ 生成片元 → 深度/颜色写入帧缓冲。
- 屏幕是离散像素阵列；采样三角形覆盖函数得到像素颜色。实际实现常用包围盒遍历和叉积符号测试。
- 可见性问题不能仅靠绘制顺序解决，需要深度缓冲（Lecture 06/07）。

### 06 抗锯齿与 Z-Buffer

- 采样不足会产生 aliasing：空间锯齿、图像 Moire、时间上的 wagon-wheel 假象。
- 频域观点：高频信号需要更高采样率；预滤波（先模糊/积分，再采样）可抑制混叠。
- 卷积定理：空间域卷积对应频域乘法；低通滤波用于抗锯齿和模糊。
- MSAA 的思想是一个像素内多点采样后求平均；超采样质量高但成本更大。
- Z-Buffer：每个像素保存当前最小深度 `z`。片元通过深度测试才覆盖颜色，复杂度约为 `O(片元数)`，无需对三角形全局排序。

### 07 光照、着色与图形管线

- Blinn–Phong 局部模型：`L = L_a + L_d + L_s`。
- 漫反射 Lambert：`L_d ∝ k_d I max(0,n·l)`，与观察方向无关。
- 镜面项：使用半程向量 `h = normalize(l+v)`，`L_s ∝ k_s I max(0,n·h)^p`；`p` 越大高光越窄。
- Ambient 是补偿未建模间接光的常数近似，不是物理全局光照。
- 图形管线：应用/顶点处理 → 三角形处理 → 光栅化 → 片元着色 → 帧缓冲操作 → 显示。

### 08 着色频率、纹理与重心坐标

- Flat shading：每三角形一个法线；Gouraud：顶点着色后插值颜色；Phong shading：插值法线后逐像素计算光照（不要与 Blinn–Phong 反射模型混淆）。
- 顶点法线常由相邻面的面积/角度加权平均得到。
- 重心坐标 `α,β,γ` 满足 `α+β+γ=1`；三角形内非负。任意顶点属性 `V=αV_A+βV_B+γV_C`。
- 纹理坐标、颜色、法线都可用重心坐标插值；透视投影后应使用透视校正插值。

### 09 纹理过滤与阴影映射

- 放大（magnification）：最近邻简单但块状；双线性使用 4 个 texel；双三次更平滑但更贵。
- 缩小（minification）：一个像素覆盖许多 texel，点采样会混叠。Mipmap 预生成逐级降采样纹理，按 footprint 选择层级并三线性插值。
- 纹理不仅存颜色，还可存环境光、法线/高度（bump/normal mapping）、程序化数据和体数据。
- Shadow mapping 两遍：从光源渲染深度图；从相机渲染时把可见点投影回光源并比较深度。它是图像空间算法，会有分辨率、自阴影和偏移（bias）伪影。

### 10 几何表示与纹理应用

- 隐式表示：`f(x,y,z)=0`（球、平面等），便于求交和内外测试。
- 显式表示：点云、参数曲线/曲面、三角形/四边形网格；网格是最常见的可渲染和可处理表示。
- OBJ 等格式记录顶点、法线、纹理坐标和面索引。
- 环境贴图（球面图、立方体图）把方向映射到远处光照；高度/法线贴图以低成本模拟细节。

### 11 Bezier 曲线与曲面

- de Casteljau：对控制点反复线性插值，参数 `t∈[0,1]` 时递归得到 Bezier 曲线点。
- 代数形式：`B(t)=Σ_{i=0}^n C(n,i)(1-t)^{n-i}t^i P_i`（Bernstein 基）。
- 端点插值；端点切线沿首尾控制边；仿射变换可直接作用于控制点；曲线位于控制点凸包内。
- Bezier 曲面是二维参数域上的张量积形式；三角形与四边形网格是曲面离散化基础。
- 进一步主题包括 B-spline、NURBS、曲面拼接和参数连续性。

### 12 曲面细分、简化与阴影

- Loop subdivision（三角网格）：每个三角形分成四个，再按权重更新新/旧顶点，逐步得到平滑曲面。
- Catmull–Clark（一般网格）：加入面点、边点并连接；非四边形区域形成 extraordinary vertex，极限曲面趋于光滑。
- 网格简化通过 edge collapse 减少面数；Quadric Error Metric 以到原始平面的距离平方和评估误差，贪心地合并代价最小的边。
- 阴影映射要点：光源深度图 + 相机可见点的光源空间深度比较；需处理 aliasing 和 shadow acne。

### 13 Whitted 光线追踪

- 针孔相机为每个像素生成射线 `r(t)=o+td`；求射线与场景的最近正交交点。
- 球面求交转为二次方程；隐式曲面求 `f(o+td)=0`；三角形求交可用平面交点 + 重心坐标（Möller–Trumbore）。
- Whitted 递归：可见性射线 + 阴影射线 + 理想镜面反射/折射射线；达到递归深度或命中光源时终止。
- 优点是镜面反射和折射直观；缺点是不能正确表示漫反射间接光和复杂全局光照。

### 14 加速结构与辐射度量

- AABB 是三对 slab 的交集；先与包围盒求交，可跳过其中所有物体。
- 空间划分：均匀网格、BSP/KD-tree、Octree；按空间切分，遍历射线穿过的单元。
- BVH 按物体集合递归构建包围盒；常按最长轴和物体中位数切分。空间树允许物体跨节点，BVH 每个物体通常只归属一个叶节点。
- 辐射度量：radiant flux `Φ`（功率）、intensity `I=dΦ/dω`、irradiance `E=dΦ/dA`、radiance `L=d²Φ/(dA_proj dω)`。Radiance 是沿射线传输和渲染计算的核心量。

### 15 光传输与全局光照

- 反射方程：`L_o(p,ω_o)=∫_H f_r(p,ω_i,ω_o)L_i(p,ω_i) cosθ_i dω_i`。
- 渲染方程在反射方程基础上加入自发光 `L_e`，递归描述光在场景中的多次反弹。
- BRDF 约束：非负、能量守恒、互易性（在适用条件下）。
- 全局光照包含间接漫反射、软阴影、颜色渗透等；解析求积通常不可行，需要 Monte Carlo。

### 16 Monte Carlo 路径追踪

- Monte Carlo 积分：从概率密度 `p(x)` 采样，`∫f(x)dx ≈ (1/N)Σ f(X_i)/p(X_i)`；误差随 `1/√N` 收敛。
- 路径追踪从相机出发，在每次交点按 BRDF 采样新方向，累积 throughput；命中光源时贡献辐射。
- Russian roulette 以概率终止长路径并对存活路径补偿权重，避免固定最大深度带来的偏差。
- 像素内多路径采样可降低噪声；重要性采样（按余弦或 BRDF）比均匀半球采样更有效。

### 17 材质与 BRDF

- Lambert 漫反射：各方向均匀反射；理想镜面遵循反射定律。
- 折射遵循 Snell 定律；入射角超过临界角时发生全反射。
- Fresnel 项表示反射率随入射角变化，掠射角反射更强；导体还需考虑复折射率和偏振。
- Microfacet 模型：宏观粗糙、微观镜面；BRDF 由法线分布（粗糙度）、Fresnel 和几何遮蔽项共同决定。各向同性/各向异性取决于微结构方向性。

### 18 高级光传输与外观

- 无偏方法：双向路径追踪（BDPT）、Metropolis Light Transport（MLT）。有偏但一致的方法：Photon Mapping、VCM；Instant Radiosity/VPL 属于 many-light 方法。
- Photon Mapping 两阶段：从光源发射并存储 photon；相机路径在邻域内做密度估计。有限样本有偏，样本趋于无穷时一致。
- VCM 结合路径连接与 photon merging，适合复杂 SDS（specular-diffuse-specular）路径。
- 高级外观：参与介质（雾/云）、头发（Kajiya–Kay、Marschner）、半透明 BSSRDF、布料、颗粒和程序化材质。

### 19 相机、镜头与光场

- 成像 = 合成（rendering）+ 捕获（camera）。针孔模型把空间点投影到传感器；透镜通过折射聚焦。
- 视场角由焦距和传感器尺寸共同决定；同一传感器下焦距越短 FOV 越大。传感器变小需缩短焦距才能保持 FOV。
- 曝光近似 `H=T×E`：快门时间 `T` 控制运动模糊；光圈/焦距影响传感器照度 `E` 和景深。
- 薄透镜模型解释焦点、失焦圆和景深；真实镜头还存在球差、色差等像差。
- 光场记录“位置 + 方向”的光线集合，可用于重聚焦、视点合成和计算摄影。

### 20 颜色与视觉感知

- **光场/光度函数收束**：全光函数以位置、方向、时间和波长参数化所有可见光；静态、非色散场景中，一条光线可用 4D（二维位置 + 二维方向）光场表示。双平面 `(s,t)-(u,v)` 参数化便于存储和新视点合成。
- **光场相机**：微透镜阵列把每个传感器像素扩展为一组方向样本，在空间分辨率和角分辨率之间做固定权衡；可在拍摄后计算重对焦、改变虚拟视点/光圈，但分辨率、成本和数据量较高。
- **光的物理表示**：光是不同波长（频率）的电磁辐射。光源的谱功率分布（SPD）描述各波长能量；颜色不是光的普适属性，而是人类视觉系统对光谱的感知结果。
- **人眼感知**：杆状细胞负责低照度下的明暗；S/M/L 三类视锥细胞对短/中/长波段响应。大脑接收的是三个响应值，而不是完整光谱。
- **同色异谱（metamerism）**：不同的无限维光谱只要产生相同的 S/M/L 响应，人眼就会认为颜色相同。这使三基色显示器可以复现许多真实光源的感知颜色，而不必复制完整光谱。
- **颜色匹配**：对基色光调整强度并相加，可用三个标量描述匹配颜色。CIE RGB 匹配函数把任意光谱 `s(λ)` 投影到 RGB 三个坐标（对波长积分）；出现负匹配量时，表示把某基色移到测试光一侧相加。
- **颜色空间**：sRGB 是广泛使用、色域有限的设备颜色空间；CIE XYZ 使用假想的 X/Y/Z 基色，匹配函数为正且覆盖可见颜色，`Y` 表示亮度。色度可用 `x=X/(X+Y+Z)`、`y=Y/(X+Y+Z)` 表示。
- **感知型空间**：HSV 用色调、饱和度、明度组织颜色；CIELAB 用 `L*`（明度）、`a*`（红–绿）、`b*`（黄–蓝）近似感知均匀性，体现颜色对立过程。
- **加色与减色**：显示器采用 RGB 加色混合；印刷采用 CMYK 减色模型，C/M/Y 吸收光，K（黑版）用于更深黑色和降低彩色油墨成本。颜色空间都有有限 gamut，超出色域的颜色需要映射。

### 21 动画与弹簧系统

- 动画是随时间变化的场景模型；电影约 24 fps、视频约 30 fps、VR 常需 90 fps。
- 关键帧：动画师指定关键状态，中间帧由插值生成；逐参数线性插值常不够平滑，可使用 spline/easing。
- 物理动画以力和质量产生加速度，适合布料、绳、头发和流体等动态系统。
- 弹簧力（Hooke）：`F=-k(|x|-l_0) n`；阻尼模拟能量损失。内部阻尼应作用于相对伸缩速度，避免抑制整体平移。
- 运动学、骨骼绑定（rigging）和动画曲线是从建模到可控角色运动的桥梁。

### 22 数值积分、刚体与流体

- 单粒子 ODE：`dx/dt=v`、`dv/dt=a(x,v,t)`。显式 Euler：`x_{t+Δt}=x_t+v_tΔt`，`v_{t+Δt}=v_t+a_tΔt`。
- Euler 一阶、易实现但误差累积且可能不稳定；减小时间步、半隐式 Euler、Verlet/RK 等可改善稳定性与精度。
- 刚体模拟需维护质心平移和角动量/旋转，处理碰撞约束和摩擦；四元数可避免 Euler 角万向节锁。
- 流体模拟通常基于质量守恒、动量方程和不可压约束；常见路线包括粒子法（SPH）与网格法（Navier–Stokes）。

## 高频公式与算法速查

| 类别 | 记忆点 |
|---|---|
| 变换 | `MVP` 右乘先执行；透视除法 `p_ndc=p_clip/w` |
| 漫反射 | `max(0,n·l)`（Lambert 余弦定律） |
| 重心坐标 | `α+β+γ=1`；属性按 `αA+βB+γC` 插值 |
| 深度 | 每像素保存最近 `z`，片元通过才写入 |
| 射线 | `r(t)=o+td`，取最小正根 |
| AABB | 三个轴 slab 区间取交集 |
| 渲染方程 | `L_o=L_e+∫ f_r L_i cosθ dω` |
| Monte Carlo | `I≈(1/N)Σ f(X_i)/p(X_i)`，误差 `O(1/√N)` |
| 弹簧 | `F=-kΔl n`，阻尼用于耗散能量 |

## 作业/实践对照（按讲义公告）

- HW0：变换与基础框架。
- HW1：光栅化与旋转三角形。
- HW2：Z-Buffer。
- HW3：Blinn–Phong 着色、插值与纹理。
- HW4：Whitted 光线追踪。
- HW5：辐射度量/光传输基础。
- HW6：加速结构（AABB、BVH）。
- HW7：Monte Carlo 路径追踪。

## 术语索引

`affine transformation` 仿射变换 · `aliasing` 混叠 · `BRDF` 双向反射分布函数 · `BVH` 层次包围盒 · `barycentric coordinates` 重心坐标 · `Fresnel` 菲涅耳项 · `frustum` 视锥体 · `irradiance` 辐照度 · `radiance` 辐亮度 · `rasterization` 光栅化 · `rendering equation` 渲染方程 · `shadow mapping` 阴影映射 · `subdivision` 曲面细分 · `texture filtering` 纹理过滤 · `z-buffer` 深度缓冲。
